--!strict

-- Emit.lua
-- Handles particle emission logic for vfx-lib.

local Types = require("./Types.lua")

local Emit = {}


function Emit:emit(particleEmitterOrMeshEmitter: ParticleEmitter | Types.MeshEmitter): ()
	
end

return Emit
